'use strict';

function InspireMail (css_selector, init_options) {

    // Arguments checking
    if (!css_selector)
        throw IMError('InspireMail must be instanciated with a CSS selector as first argument.')

    var _editor_container = css_selector;

};
